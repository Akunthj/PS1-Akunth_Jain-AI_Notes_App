# requirements.txt
# This file lists all Python packages our backend needs.
# Install them all at once with: pip install -r requirements.txt

fastapi==0.111.0       # The web framework
uvicorn==0.29.0        # The server that runs FastAPI (like how Apache runs PHP)
httpx==0.27.0          # HTTP client — used to call the Anthropic API
python-dotenv==1.0.1   # Lets us load environment variables from a .env file
python-jose==3.3.0     # JWT tokens — used to create and verify login sessions
passlib==1.7.4         # Password hashing — NEVER store plain text passwords
bcrypt==4.1.3          # The hashing algorithm passlib uses under the hood
google-generativeai